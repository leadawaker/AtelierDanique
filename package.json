{
  "name": "atelier-danique-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs && node scripts/check-seo.mjs",
    "preview": "vite preview",
    "check-seo": "node scripts/check-seo.mjs",
    "import-photos": "node scripts/import-design-photos.mjs"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.4",
    "@vercel/blob": "^2.8.0",
    "@vimeo/player": "^2.30.4",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  }
}

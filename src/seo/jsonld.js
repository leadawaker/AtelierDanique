// Stub. Task 4 fills this in with real structured data (LocalBusiness,
// Product/Offer, FAQPage, BreadcrumbList).
export function jsonLd() { return { '@context': 'https://schema.org', '@graph': [] }; }

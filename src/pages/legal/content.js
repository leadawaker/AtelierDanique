import { PRIVACY } from './privacy.js';
import { TERMS } from './terms.js';

// The two legal pages by name, each with one version per language (en, pt, nl).
export const LEGAL = { privacy: PRIVACY, terms: TERMS };
